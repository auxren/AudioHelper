<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for notarization with hardened runtime -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Needed so Python extensions (.so) load without quarantine issues -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>

    <!-- File system access for processing audio from external drives -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Allow subprocess (ffmpeg, flac, afplay) -->
    <key>com.apple.security.temporary-exception.sbpl</key>
    <array>
        <string>(allow process-exec)</string>
    </array>
</dict>
</plist>
