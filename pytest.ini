[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
markers =
    needs_ffmpeg: test requires ffmpeg/ffprobe on PATH
    needs_mutagen: test requires the mutagen package
